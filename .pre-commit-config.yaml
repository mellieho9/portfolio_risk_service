repos:
  # Ruff: lint (with autofix) + format
  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.4.4
    hooks:
      - id: ruff
        args: [--fix]
      - id: ruff-format

  # mypy: strict type checking
  - repo: https://github.com/pre-commit/mirrors-mypy
    rev: v1.10.0
    hooks:
      - id: mypy
        args: [--strict]
        additional_dependencies:
          - types-requests
          - types-python-dateutil

  # bandit: security analysis
  - repo: https://github.com/PyCQA/bandit
    rev: 1.7.8
    hooks:
      - id: bandit
        args: [-ll, -r, app/, training/]

  # vulture: dead code detection
  - repo: https://github.com/jendrikseipp/vulture
    rev: v2.11
    hooks:
      - id: vulture
        args: [app/, training/, --min-confidence, "80"]

  # eradicate: remove commented-out dead code
  - repo: https://github.com/wemake-services/eradicate
    rev: 2.3.0
    hooks:
      - id: eradicate

  # Comment density: fail if > 20% of lines are # comments
  - repo: local
    hooks:
      - id: check-comments
        name: comment density
        entry: python scripts/check_comments.py
        language: python
        types: [python]

  # General hygiene
  - repo: https://github.com/pre-commit/pre-commit-hooks
    rev: v4.6.0
    hooks:
      - id: check-merge-conflict
      - id: check-added-large-files
        args: [--maxkb=500]
      - id: detect-private-key
      - id: end-of-file-fixer
      - id: trailing-whitespace
